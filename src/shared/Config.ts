export default {
  MAX_PREVIEW_FILE_SIZE: 0.5 * 1024 * 1024, // = 0.5mb
  GITIGNORE_RECOMMENDED: ['node_modules', '.idea', '.vscode', '.DS_Store', 'target'],
  TERMINAL_INPUT_PLACEHOLDER: 'Enter a command...',
  TERMINAL_LOG_FORMAT: '[{level}] {timestamp}: {message}',
  WINSTON_LOG_FORMAT: '[{level}] {timestamp}: {message}',
  WINSTON_TIME_FORMAT: 'DD.MM.YYYY | HH:mm:ss',
  PATH_REGEX: />"(.*)"</gm
}
